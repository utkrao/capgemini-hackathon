import { Base, Type } from "igniteui-react-core";
import { IRegionAddress } from "./IRegionAddress";
/**
 * @hidden
 */
export declare class RegionLookupTable_RangeHelper$1<T extends IRegionAddress> extends Base {
    static $t: Type;
    protected $t: Type;
    private g;
    private h;
    private i;
    private j;
    private readonly e;
    private readonly d;
    private readonly c;
    constructor($t: Type);
    k(): void;
    f(a: boolean, b: number, c: number, d: number, e: number): (arg1: T) => boolean;
    a(a: T): boolean;
    b(a: T): boolean;
}
